# Cloudflare Pages 构建设置
# 在 Cloudflare Dashboard > Pages > coidea-ai > Settings > Build & Deploy 中配置

# 构建命令
npm install --legacy-peer-deps && npm run build

# 构建输出目录
frontend/build

# 环境变量
NODE_VERSION=20
